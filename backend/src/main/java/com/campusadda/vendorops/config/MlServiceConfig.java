package com.campusadda.vendorops.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class MlServiceConfig {
    // Keep for future RestTemplate/WebClient beans if needed.
}